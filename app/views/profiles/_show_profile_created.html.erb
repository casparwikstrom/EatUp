<% if @popups.present? %>
    <% @popups.each do |popup| %>
    <div>
    <%= popup.title %>
    <%= link_to "Delete", popup_path(popup), method: :delete, data: { confirm: "Are you sure?" } %>
    </div>
    <% end %>

<% else %>
  <h1>You haven't added anything yet yooo</h1>
<% end %>
